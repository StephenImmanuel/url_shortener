<table class="table col-xs-7">
 <tr>
   <th>Url:</th>
   <td><%= @shortened_url.url %></td>
 </tr>
 <tr>
   <th>Short url:</th>
   <td><%= link_to @shortened_url.short_url, @shortened_url.url, :target => '_blank' %></td>
 </tr>
</table>

<%= link_to 'Create Shortened url', new_shortened_url_path, class: 'btn btn-primary' %>
