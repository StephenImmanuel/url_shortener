== README

URL Shortener application usage documentation:

* Run bundle install, to install the dependencies

* Run rake db:migrate

* This application uses sqlite3 database

* Use rails s to start the application server

* Use rake to run all the test
